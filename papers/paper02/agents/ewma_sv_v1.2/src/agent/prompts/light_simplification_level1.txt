Review the current world model and generalize and simplify it.

Assume the real game mechanics are simpler than the current implementation.
 
Re-check the relevant ASCII/PNG frames and ask what simpler mechanic, object family, state variable, or parameterized rule would make the special handling unnecessary. Remove distinctions not forced by evidence and replace case-by-case behavior with shared rules.

The level objective and core mechanics should be explainable in a few short sentences. Simplify the code and `world_model.md` together so reconstruction, rendering, state transitions, and planning all follow from the same clean underlying model. Test simpler hypotheses against recorded observations, then keep the simplest explanation that fits.
